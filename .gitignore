node_modules/
.next/
out/
build/
cache/
artifacts/
typechain-types/
.env
.env.local
__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
# Local-chain address books are ephemeral; testnet/mainnet ones are part of the submission
# and must be committed so reviewers can find the deployed contracts.
deployments/hardhat.json
deployments/localhost.json
coverage/
coverage.json
.deployer-address
deployments/simulation.json
tsconfig.tsbuildinfo
.next-build
.next-deploy

# Local probe/scratch artifacts
.next-probe*
*_tmp.js

# Runtime state: relayer cursor, faucet claim book. Machine-written, not source.
var/*.json
!var/relayer-state.json

# Deck: the PDF ships, the intermediate HTML does not.
deck/deck.html
